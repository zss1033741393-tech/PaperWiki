__pycache__/
.worktrees/
*.py[cod]
.venv/
.env
.DS_Store
.paperwiki/
.obsidian/workspace.json
.obsidian/workspace-mobile.json
raw/papers/*
!raw/papers/.gitkeep
reports/*
!reports/.gitkeep
!reports/*/
reports/*/*
!reports/*/report.md
!reports/*/report.html
!reports/*/analysis.json
!reports/*/record.json
reading-lists/*
!reading-lists/.gitkeep
!reading-lists/*.json
reading-lists/latest.json
reading-lists/recommended-next.json
# NOTE: wiki/papers/ is intentionally NOT ignored — the deposited paper pages
# (generated synthesis + human-authored user notes + reciprocal wikilinks) are the
# durable knowledge base this tool exists to produce, so they must be version-controlled.
# validation vault: keep deposited markdown fixtures, drop reproducible downloaded PDFs
validation/fixtures/vault/raw/
